{
  "name": "PlaneSimulator",
  "version": "1.0.0",
  "description": "CSS Grid project",
  "main": "index.js",
  "scripts": {
    "start": "browser-sync start --server --files '**/*.css, **/*.html, **/*.js' --port 7777 --open http://localhost:7777/index.html --browser chrome"
  },
  "author": "mavalos",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.27.10"
  }
}
